# Core orchestration
langchain>=1.0.0
langgraph>=0.2.0

# Ingestion pipeline
websockets>=12.0
certifi>=2024.0.0
asyncpg>=0.29.0
PyYAML>=6.0

# PostgreSQL sync access (indexing path via cjutil SqlStore)
psycopg>=3.1.0

# Embedding/indexing runtime used by current index CLI defaults
sentence-transformers>=2.7.0
transformers>=4.0.0
